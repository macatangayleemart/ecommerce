<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Cart</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #c3ecf5 0%, #a5dee5 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            font-family: 'Arial', sans-serif;
        }
        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
        }
        h1 {
            text-align: center;
            color: #005f73; /* Dark teal */
            margin-bottom: 30px;
            font-weight: bold;
        }
        .card {
            border: none;
            border-radius: 15px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }
        .card-body {
            background-color: #f0f4f8; /* Light gray */
            border-radius: 15px;
        }
        .card-title {
            color: #005f73; /* Dark teal */
            font-weight: bold;
        }
        .btn-danger {
            background-color: #ff6b6b; /* Light red */
            border: none;
            border-radius: 15px;
            width: 100%;
            transition: background-color 0.3s ease;
        }
        .btn-danger:hover {
            background-color: #ff3b3b; /* Darker red */
        }
        .btn-success, .btn-secondary {
            border-radius: 15px;
            width: 100%;
            padding: 12px;
            transition: background-color 0.3s ease;
        }
        .btn-success {
            background-color: #5cb85c; /* Green success */
        }
        .btn-success:hover {
            background-color: #449d44; /* Darker green */
            color: white;
        }
        .btn-secondary {
            background-color: #f0f4f8; /* Light gray */
            border: 1px solid #a5dee5; /* Light teal border */
            color: #005f73; /* Dark teal */
        }
        .btn-secondary:hover {
            background-color: #a5dee5; /* Light teal */
            color: white;
        }
        .text-center a {
            margin-top: 10px;
        }
        .empty-cart {
            text-align: center;
            color: #6c757d; /* Gray */
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Your Cart</h1>
        <div class="row">
            <% if (cart.length > 0) { %>
                <% cart.forEach(item => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title"><%= item.name %></h5>
                                <p class="card-text">Price: $<%= item.price.toFixed(2) %></p>
                                <p class="card-text">Quantity: <%= item.quantity %></p>
                                <form action="/cart/remove" method="POST">
                                    <input type="hidden" name="productId" value="<%= item.id %>">
                                    <button type="submit" class="btn btn-danger">Remove from Cart</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
                <!-- Add Checkout Button -->
                <div class="col-md-12 text-center mt-4">
                    <a href="/checkout" class="btn btn-success">Proceed to Checkout</a>
                </div>
            <% } else { %>
                <div class="col-md-12 empty-cart">
                    <p>Your cart is empty.</p>
                </div>
            <% } %>
        </div>
        <div class="text-center mt-4">
            <a href="/clientdashboard" class="btn btn-secondary">Continue Shopping</a>
        </div>
    </div>
</body>
</html>
