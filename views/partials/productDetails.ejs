<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom Styles -->
    <style>
        body {
            background: linear-gradient(135deg, #f0f4f8, #d9e2ec); /* Soft gradient background */
            font-family: 'Arial', sans-serif;
        }
        .container {
            margin-top: 50px;
            max-width: 800px;
            background-color: #ffffff; /* White background for the container */
            padding: 30px;
            border-radius: 12px; /* Softer rounded corners */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1); /* Softer shadow */
        }
        h1 {
            font-size: 2.8rem;
            color: #004d40; /* Teal for the main title */
            text-align: center;
            margin-bottom: 30px;
            font-weight: 700;
        }
        h2 {
            color: #00796b; /* Lighter teal for the price */
            font-weight: 700;
            text-align: center;
            margin-bottom: 30px;
        }
        img {
            display: block;
            margin: 0 auto;
            max-width: 100%;
            height: auto;
            border-radius: 10px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1); /* Shadow for the image */
        }
        p {
            font-size: 1.2rem;
            color: #455a64; /* Dark gray for the text */
            margin-top: 25px;
            text-align: justify;
        }
        .btn {
            display: block;
            width: 100%;
            margin: 20px 0;
            padding: 12px 0;
            font-size: 1.2rem;
            border-radius: 5px;
            transition: background-color 0.3s ease, transform 0.2s ease; /* Smooth transition */
        }
        .btn-primary {
            background-color: #00796b; /* Teal for the primary button */
            border: none;
        }
        .btn-primary:hover {
            background-color: #004d40; /* Darker teal on hover */
            transform: scale(1.02); /* Slight zoom on hover */
        }
        .btn-secondary {
            background-color: #607d8b; /* Medium gray for the secondary button */
            border: none;
            color: white; /* White text for better contrast */
        }
        .btn-secondary:hover {
            background-color: #455a64; /* Darker gray on hover */
            transform: scale(1.02); /* Slight zoom on hover */
        }
    </style>
</head>
<body>

    <div class="container">
        <h1><%= product.name %></h1>
        <img src="<%= product.image_url %>" alt="<%= product.name %>">
        <h2>Price: $<%= product.price.toFixed(2) %></h2>
        <p><%= product.description %></p> <!-- Assuming you have a description field -->
        
        <form action="/cart/add" method="POST">
            <input type="hidden" name="productId" value="<%= product.id %>">
            <button type="submit" class="btn btn-primary">Add to Cart</button>
        </form>
        <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
    </div>

</body>
</html>
